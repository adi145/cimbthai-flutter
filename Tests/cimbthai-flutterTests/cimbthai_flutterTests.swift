import XCTest
@testable import cimbthai_flutter

final class cimbthai_flutterTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(cimbthai_flutter().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
